# wallet-adapter
Multi-Chain Wallet Adapter is a lightweight and extensible Go library that provides a unified interface for transaction construction, signing (via external MPC), and broadcasting across multiple blockchain networks.
